<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>$(Framework)</TargetFramework>
    <AssemblyName>lockedHostTemp</AssemblyName>
    <PackageId>lockedHostTemp</PackageId>
    <RuntimeIdentifiers>$(PackageTargetRid)</RuntimeIdentifiers>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NETCore.DotNetHostResolver" Version="$(HostFullVersion)" />
    <PackageReference Include="Microsoft.NETCore.DotNetHost" Version="$(HostFullVersion)" />
  </ItemGroup>

</Project>
